import { FlowRunner } from './flow-runner';

describe('flow-runner', () => {
  describe('FlowRunner', () => {
    it('should be instantiable', () => {
      // TODO: Add proper constructor arguments
      expect(FlowRunner).toBeDefined();
    });
  });

});
