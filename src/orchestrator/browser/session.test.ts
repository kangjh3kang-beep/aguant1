import { BrowserSession } from './session';

describe('session', () => {
  describe('BrowserSession', () => {
    it('should be instantiable', () => {
      // TODO: Add proper constructor arguments
      expect(BrowserSession).toBeDefined();
    });
  });

});
